Oi! Você está na tela inicial do sistema.

<% if policy(User).create? %>
  <p><%= link_to users_path do %>Manage Users<% end %></p>
<% end %>

<% if policy(Location).create? %>
  <p><%= link_to locations_path do %>Manage Locations<% end %></p>
<% end %>

<% if policy(Product).index? %>
  <p><%= link_to products_path do %>Manage Products<% end %></p>
<% end %>

<% if policy(Offering).index? %>
  <p><%= link_to offerings_path do %>Manage Offerings<% end %></p>
<% end %>

<% if policy(Harvest).create? %>
  <p><%= harvest_link(@current_harvest) %></p>
<% end %>

<%= button_to 'Logout', logout_path, method: :delete, data: { confirm: 'Are you sure you want to log out?' } %>
